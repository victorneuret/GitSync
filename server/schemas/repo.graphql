type Repo {
    name: String!
    private: Boolean!
    githubURL: String!
    owner: String!
    updater: String!
}

input NewRepo {
    name: String!
    private: Boolean!
    owner: String!
}

input ModifRepo {
    private: Boolean!
    updater: String!
}